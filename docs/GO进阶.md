## Go语言实践 - Runtime
  - Go语言中Goroutine的调度原理
  ```
     Goroutine 的实现、GPM调度模型、调度状态即流转、调度原理、协作式抢占以及和网络库的协作
  ```
  - Go语言中的内存模型
  ```
     Go内存分配的内部结构和分配机制
  ```
  - Go语言中GC(垃圾回收)的原理
  ```
     Go GC介绍、三色标记的实现原理、GC的流程以及GC的优化方案
  ```
  - Go语言中channel的消息通信原理
  ```
     Go channel的通讯机制，环形队列的结构、调度和唤醒的原理
  ```

## Go语言实践 - 并行编程
  - Go语言中Memory Model
  ```
     内存模型：Happens Before、同步语义、channel通讯、锁
  ```
  - Go语言中并发特征并行编程模式: Timeout,Pipeline、Cancellation、Fanout、errgroup 等模型
  ```
     利用channel完成并行开发的设计模式，超时控制、管道、扇出、errgroup并发
  ```
  - Go语言中Context原理
  ```
     使用Go标准Context的原理和最佳实践，包含超时控制、元数据传递、声明周期控制
  ```

## Go语言实践 - 网络编程
  - Go语言中TCP网络编程
  ```
     TCP Server基础库和性能优化方案
  ```
  - Go语言的HTTP网络编程
  ```
     HTTP Server 基础库和框架
  ```

## Go语言实践 - 异常处理
  - Go语言中error处理方法
  ```
     error处理实践：检查错误、定义错误、追加上下文
  ```
  - Go语言中业务错误的处理方法

## Go工程化实践
  - Go项目中项目目录组织原则和规范
  - Go项目中API设计方法和规范
  - Go项目中Package的管理和设计方法
  - Go项目中单元测试方法
  ```
     go test工具链使用
     Mock技术
  ```

## Go架构实践 - 分布式架构(前端负载均衡)
  - 高可用DNS
  ```
     DNS原理
     防劫持的方法
     HTTP DNS + IP 长连接
  ```
  - CDN架构和应用场景
  ```
     CDN系统架构
     保证数据一致性
  ```
  - 4/7层负载均衡的原理
  ```
     LVS、Nginx 4/7 层负载均衡原理
  ```

## Go架构实践 - 分布式架构 (数据分片)
  - 数据Sharding设计原则
  ```
     分库分表
     多活的Shard设计
  ```
  - Hash分片的算法和演进
  ```
     Hash求余
     一致性Hash
     有界负载一致性Hash
     节点映射
  ```

## Go架构实践 - 微服务 (微服务概览与治理)
  - 微服务的演进历史以及优缺点
  - 微服务的设计方法
  - 微服务中RPC底层原理

## Go架构实践 - 微服务(可用性设计)
  - 可用性设计关键点：隔离、超时控制、过载保护、限流、容错、重试
  ```
     1. 微服务的隔离实现、以及架构设计中的隔离实现
     2. 进程内超时控制和跨进程超时控制
     3. 程序自保护避免过载、抛弃一定的流量完成自适应限流
     4. 单机限流、多租户场景和分布式限流
     5. 节点故障的容错逻辑、重试容错的策略和设计
  ```

## Go架构实践 - 中间件(日志、指标、链路追踪)
  - 日志收集
  - 监控指标体系:  Prometheus
  - 分布式链路追踪 Jaeher:分布式链路追踪

## Go架构实践 - 中间件(缓存、数据库)
  - Redis Memcache 
  ```
     缓存一致性设计
  ```
  - MySQL常用设计和优化
  ```

  ```

## Go架构实践 - 中间件(消息队列、服务发现)
  - 消息队列原理 掌握基于消息队列的架构设计方法
  - 服务发现原理、选型策略、以及服务发现实现的微服务多租户架构

```markdown
1. 保持空杯心态，不要排斥


学到的就要教人，赚到的就要给人
```
